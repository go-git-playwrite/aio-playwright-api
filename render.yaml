services:
  - type: web
    name: playwright-api-2
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: 0
